services:
  frontend:
    build: { context: ., dockerfile: Dockerfile }
    ports:
      - "3000:3000"
    environment:
      CHOKIDAR_USEPOLLING: "true"
      CHOKIDAR_INTERVAL: "1000"
    volumes:
      - .:/app
      - sastur_modules:/app/node_modules

volumes:
  sastur_modules:
